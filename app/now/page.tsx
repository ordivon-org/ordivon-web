import type { Metadata } from "next";
import Link from "next/link";
import { projects } from "@/lib/projects";
export const metadata: Metadata = { title: "Current", description: "The active Ordivon questions and current evidence frontier as of 29 July 2026." };
export default function NowPage() { return <div className="page-shell page-top now-page"><header className="index-hero"><p className="eyebrow">Current · 29 July 2026</p><h1>The next useful result may be a capability, a deletion, or a changed boundary.</h1><p>This is an evidence frontier, not a promise of dates. Priorities move when implementation and repeated use change the underlying model.</p></header><section className="now-list">{projects.map((project)=><Link href={`/projects/${project.slug}`} key={project.slug}><span>{project.index}</span><div><p>{project.group}</p><h2>{project.title}</h2><strong>{project.openQuestions[0]}</strong></div><b>↗</b></Link>)}</section><section className="now-discipline"><p className="section-index">Current discipline</p><h2>Prefer observed use over speculative expansion.</h2><p>New abstractions should begin with a concrete failure, missing explanation, or recurring need. Until then, the preferred moves are to use mature systems, improve the public explanation, remove stale structure, and keep uncertainty explicit.</p></section></div>; }
